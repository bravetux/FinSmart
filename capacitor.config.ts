import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cozywalrussnap',
  appName: 'cozy-walrus-snap',
  webDir: 'dist'
};

export default config;
